@using BankOrderSys.Models
@model BankOrderSys.Models.OrderFormView

@{
    ObjectIncasation item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
    item = new ObjectIncasation();
    Model.obj_list.Add(item);
}


<div class="col-md-12" style="overflow-x:scroll">
    <table class="table table-striped table-bordered">
        <col width="20%">
        <thead>
            <tr>
                <th>Описание</th>
                @{
                    int i = 0;
                }

                @foreach (var a in Model.obj_list){
                    <th>Объект №@(++i)</th>
                }
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><b>Желательное время сдачи наличных (самостоятельно)/ время прибытия инкассаторов в организацию</b></td>
                @foreach (var a in Model.obj_list){
                    <td>@a.time</td>
                }
            </tr>
            <tr>
                <td><b>Способ сдачи денежной наличности</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.type_get_money</td>
                }
            </tr>
            <tr>
                <td><b>Периодичность оказания инкассаторских услу</b>г</td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.period</td>
                }
            </tr>
            <tr>
                <td><b>День недели</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.week_day</td>
                }
            </tr>
            <tr>
                <td><b>Предполагаемый объем денежной наличности для инкассации</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.count_money</td>
                }
            </tr>
            <tr>
                <td><b>Код валюты</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.code_money</td>
                }
            </tr>
            <tr>
                <td><b>Руководитель объекта</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.boss_object</td>
                }
            </tr>
            <tr>
                <td><b>Желательная дата начала обслуживания/Дата открытия торговой точки клиента</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.boss_object</td>
                }
            </tr>
            <tr>
                <td><b>Рабочие с</b></td>
            </tr>
            <tr>
                <td><b>Субота</b></td>
            </tr>
            <tr>
                <td><b>Воскресенье с</b></td>
            </tr>
            <tr>
                <td><b>Тип адреса</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.type_adress</td>
                }
            </tr>
            <tr>
                <td><b>Тип населенного пункта</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.type_city</td>
                }
            </tr>
            <tr>
                <td><b>Наименование населенного пункта</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.title_city</td>
                }
            </tr>
            <tr>
                <td><b>Улица</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.title_street</td>
                }
            <tr>
                <td><b>Номер дома (владение)</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.num_build</td>
                }
            </tr>
            <tr>
                <td><b>Корпус (строение)</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.title_part</td>
                }
            </tr>
            <tr>
                <td><b>Инкассаторская услуга</b></td>
                @foreach (var a in Model.obj_list)
                {
                    <td>@a.type_order</td>
                }
            </tr>
        </tbody>
    </table>
</div>